// Program that reads a string and converts it to uppercase.

// if str = "HemaNATH"

// output 

// HEMANATH

const str = "balAJIS";

if(str != null){

    console.log("The given string convereted into Uppercase " + str.toUpperCase());

    console.log("The given string convereted into Lowercase " + str.toLowerCase());
    
}